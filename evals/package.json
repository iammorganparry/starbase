{
  "name": "@starbase/evals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Eval harnesses \u2014 real-model measurement, never run in CI.",
  "scripts": {
    "planning": "tsx planning/runner.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@starbase/cli-adapters": "workspace:*",
    "@starbase/core": "workspace:*",
    "@effect/platform-node": "^0.107.0",
    "effect": "^3.21.4"
  },
  "devDependencies": {
    "@starbase/tsconfig": "workspace:*",
    "@types/node": "^22.10.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
