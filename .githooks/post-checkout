#!/bin/sh
# git passes: $1 = previous HEAD, $2 = new HEAD, $3 = 1 for a branch checkout
# (0 for a file checkout). Only sync when we actually switched branches.
[ "$3" = "1" ] || exit 0
"$(dirname "$0")/sync-deps.sh" "$1" "$2"
