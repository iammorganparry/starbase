# Electron native modules and hoisting behave best with a flat-ish store.
node-linker=hoisted
shamefully-hoist=true
# Allow electron's postinstall to download the binary.
enable-pre-post-scripts=true
